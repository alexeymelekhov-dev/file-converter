services:
#  app:
#    build:
#      context: .
#      dockerfile: Dockerfile
#    container_name: file-converter-app
#    ports:
#      - "8081:8080"
#    environment:
#      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/file_converter
#      SPRING_DATASOURCE_USERNAME: postgres
#      SPRING_DATASOURCE_PASSWORD: postgres
#      SPRING_PROFILES_ACTIVE: dev
#    depends_on:
#      postgres:
#        condition: service_healthy

  postgres:
    image: postgres:18
    restart: always
    container_name: file-converter-db
    environment:
      POSTGRES_DB: file_converter
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - pg_data:/var/lib/postgresql
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d file_converter" ]
      interval: 5s
      timeout: 5s
      retries: 10

  kafka:
    image: apache/kafka:latest
    container_name: file-converter-kafka
    ports:
      - "9092:9092"

  minio:
    image: minio/minio:latest
    container_name: file-converter-minio
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"

volumes:
  pg_data: